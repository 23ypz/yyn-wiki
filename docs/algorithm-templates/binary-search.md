# 二分总览

二分的本质是在一个具有单调性的序列或答案空间中寻找边界。

本节拆成两个部分：

- [基础二分](binary-search-basic.md)：在有序数组中查找第一个满足条件的位置。
- [二分答案](binary-search-answer.md)：在答案空间中二分，通过 `check` 判断当前答案是否可行。

二分的关键不是记模板，而是维护不变量。例如查找第一个 `>= target` 的位置时，可以维护：

$$
a[l] < target, \quad a[r] \ge target
$$

最终当 `l + 1 == r` 时，`r` 就是答案。
