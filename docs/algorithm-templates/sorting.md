# 排序总览

排序常作为贪心、二分、扫描线、区间处理等算法的预处理步骤。Python 的排序非常强大，但也需要清楚 `key` 和比较函数的含义。

本节拆成两个部分：

- [简单排序](sorting-basic.md)：`sort`、`sorted`、`key`、元组排序。
- [自定义排序](sorting-custom.md)：使用 `cmp_to_key` 描述更复杂的比较关系。

排序前最重要的问题是：你到底希望元素按照什么规则变成“有序”。
